# Auth Service environment variables
# Copy to auth.env and fill in values before running.

# HTTP
PORT=3005

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agroasys_auth
DB_USER=postgres
DB_PASSWORD=password

# Session — default lifetime in seconds (24 h)
SESSION_TTL_SECONDS=86400
