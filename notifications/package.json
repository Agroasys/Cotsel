{
  "name": "@agroasys/notifications",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.cjs",
    "prepare": "npm run build",
    "prepack": "npm run build"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": ">=4.5.0"
  }
}
